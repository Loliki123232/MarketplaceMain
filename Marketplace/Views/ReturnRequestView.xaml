<UserControl x:Class="Marketplace.Views.ReturnRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White" Padding="25" Margin="20" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="🔄 Оформление возврата" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Информация о заказе -->
            <Border Grid.Row="1" Background="#F8F9FA" Padding="15" Margin="0,0,0,15" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding OrderInfo}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ProductInfo}" FontSize="13" Foreground="#6C757D" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Количество для возврата -->
            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Text="Количество для возврата" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,10,0" Padding="8"/>
                    <TextBlock Grid.Column="1" Text="{Binding MaxQuantity, StringFormat=Макс: {0} шт}" 
                               FontSize="12" Foreground="#6C757D" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <!-- Сумма возврата -->
            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                <TextBlock Text="Сумма возврата" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding ReturnAmount, StringFormat={}{0:N2} ₽}" FontSize="18" FontWeight="Bold" Foreground="#FF5722"/>
            </StackPanel>

            <!-- Причина возврата -->
            <StackPanel Grid.Row="4" Margin="0,0,0,20">
                <TextBlock Text="Причина возврата" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" 
                         Height="80" TextWrapping="Wrap" AcceptsReturn="True" Padding="8"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Width="100" Height="35" Margin="0,0,10,0"
                        Background="#E9ECEF" Foreground="#495057" BorderThickness="0"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Отправить заявку" Width="150" Height="35" 
                        Background="#FF5722" Foreground="White" BorderThickness="0"
                        Command="{Binding SubmitCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>